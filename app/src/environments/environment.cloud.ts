export const environment = {
    production: false,
    cloudMode: true,
    preCloudMode: true,
    featurePreviewMode: false,
    protocol: 'https',
    apiUrl: window["env"]["apiUrl"], // API URL
    apiPort: 443
}
